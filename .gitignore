
!**/testdata/**/*.pem
!.env.example
#
# --- Build output ----------------------------------------------------------
# --- Editors / OS ----------------------------------------------------------
# --- Genesis working files (canonical genesis is published separately) -----
# --- Logs ------------------------------------------------------------------
# --- Runtime / node home ---------------------------------------------------
# --- Rust ------------------------------------------------------------------
# --- Secrets and keys: NEVER COMMIT ---------------------------------------
# --- Tooling caches --------------------------------------------------------
# ---------------------------------------------------------------------------
# Hashgram .gitignore
# Release artefacts. Reproducible from source with scripts/dev/release.sh, so
# Rust build output. Reproducible from source.
# SECURITY: this file is a defence-in-depth measure only. The authoritative
# `hashgramctl mainnet-preflight`. Never rely on .gitignore alone to keep a
# controls are the `gitleaks` secret scan in CI (.github/workflows/ci.yml) and
# key out of history.
# there is no reason to carry binaries in git.
**/keyhash
**/keyring-file/
**/keyring-test/
**/target/
*.a
*.db
*.exe
*.key
*.log
*.mnemonic
*.o
*.out
*.pem
*.so
*.sqlite
*.sqlite3
*.swp
*.test
*.wal
*mnemonic*
*seed-phrase*
*seedphrase*
*~
.DS_Store
.env
.env.*
.idea/
.vscode/
/.cache/
/.hashgram/
/.hashgramd/
/bin/
/build/
/devnet/
/dist/
/genesis-work/
/localnet/
/logs/
/secrets/
/testnet-data/
/vendor/
Cargo.lock.orig
coverage.html
coverage.out
genesis.json.tmp
node/target/
node_key.json
phase2-data/
priv_validator_key.json
priv_validator_state.json
target/

# Binaries built in the repo root by `go build ./cmd/<name>` land here by accident.
/hashgramd
/hashgramctl
/hashgram-keygen
/hashgram-test-client
/hashgram-indexer
/hashgram-safety
/hashgram-node
/hashgram-client

# Chain relay devnet harness state (scripts/testnet/chain-relay-devnet.ps1).
/devnet-relay/


# Frontend dependencies (apps/desktop).
node_modules/
apps/desktop/dist/
apps/desktop/src-tauri/gen/

